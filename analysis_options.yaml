# Static analysis configuration.
include: package:flutter_lints/flutter.yaml

analyzer:
  plugins:
    - custom_lint
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    # Generated code and Riverpod patterns make a few defaults noisy.
    prefer_final_locals: true
